server:
  port: 10000
spring:
  application:
    name: eurekaService
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:10000/eureka
security:
  basic:
    enabled: true
  user:
    name: ylw
    password: ylw1997